<div class="container">
  <%= simple_form_for([@plant, @plant_tag]) do |f| %>
    <%#= f.association :tag %>
    <%= f.input :tag, collection: Tag.all, input_html: { multiple: true, class: "select2" } %>
    <%= f.submit class: "btn btn-primary" %>
  <% end %>
</div>
